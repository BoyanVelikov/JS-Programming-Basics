function numbersFromNto1(input) {
    let x = Number(input);
    let s = "";
    for (let i = x; i >= 1; i--) {
        if (i > 1) {
            s = s += i + "," + " ";
        } else {
            s = s += i;
        }
    }
    console.log(s);
}
