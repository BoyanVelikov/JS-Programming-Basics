function triangleOfStars(input) {
    let height = Number(input);
    let s = "";
    for (let i = 1; i <= height; i++) {
        for (let k = 1; k <= i; k++) {
        }
        s += "*";
        console.log(s);
    }
}
