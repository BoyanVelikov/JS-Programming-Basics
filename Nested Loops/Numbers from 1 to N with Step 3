function numbersWithStep(n) {
    let result = 1;
    for (i = 1; i <= n; i += 3) {
        if (i > 1) {
            result += ', ';
            result += i;
        }
    }
    console.log(result);
}
