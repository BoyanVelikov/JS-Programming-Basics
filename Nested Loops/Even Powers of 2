function evenPowersOfTwo(n) {
    let num = 1;
    let result = "";
    for (let i = 0; i <= n; i += 2) {
        if (i > 0)
            result += ", ";
        result += num;
        num = num * 2 * 2;
    }
    console.log(result);
}
