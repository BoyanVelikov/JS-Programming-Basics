function sumNumbers(nums) {
    let a = Number(nums.length);
    let c = 0;
    for (i = 1; i <= a; i++) {
        b = nums[i - 1];
        if (b == 0) {
            console.log("Good bye");
        } else {
            c = c + b;
            console.log(`Sum = ${c}`);
        }
    }
}
