function examCountdown(days) {
    let a = Number(days);
    for (i = a; i >= 0; i--) {
        if (i > 0) {
            console.log(`${i} days before the exam`);
        } else {
            console.log("The exam has come.")
        }
    }
}
